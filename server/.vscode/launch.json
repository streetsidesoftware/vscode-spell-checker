{
	"version": "0.1.0",
	// List of configurations. Add new configurations or edit existing ones.
	"configurations": [
		{
			"name": "Attach Server",
			"type": "node",
			"request": "attach",
			// TCP/IP address. Default is "localhost".
			"address": "localhost",
			"port": 60048,
			"sourceMaps": true,
			"outFiles": [ "${workspaceRoot}/../client/server/**/*.js" ]
        },
		{
			"type": "node2",
			"request": "launch",
			"name": "Launch Tools",
			"program": "${workspaceRoot}/tools/out/app.js",
			"args": ["compile", "./dictionaries/php.txt", "./dictionaries/wordsEn.txt", "-o", "./temp"],
			"sourceMaps": true,
			"cwd": "${workspaceRoot}"
		},
        {
			// Name of configuration; appears in the launch configuration drop down menu.
			"name": "Run mocha",
			// Type of configuration. Possible values: "node", "mono".
			"type": "node2",
            // Request type "launch" or "attach"
            "request": "launch",
			// Workspace relative or absolute path to the program.
			"program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
			// Automatically stop program after launch.
			"stopOnEntry": false,
			// Command line arguments passed to the program.
			"args": ["--recursive", "../client/server/test"],
			// Workspace relative or absolute path to the working directory of the program being debugged. Default is the current workspace.
			"cwd": "${workspaceRoot}",
			// Workspace relative or absolute path to the runtime executable to be used. Default is the runtime executable on the PATH.
			"runtimeExecutable": null,
			"outFiles": [ "${workspaceRoot}/../client/server/**/*.js" ],
			"sourceMaps": true,
			// Environment variables passed to the program.
			"env": { "NODE_ENV": "test"}
		},
        {
			// Name of configuration; appears in the launch configuration drop down menu.
			"name": "Run mocha suggest.test",
			// Type of configuration. Possible values: "node", "mono".
			"type": "node",
            // Request type "launch" or "attach"
            "request": "launch",
			// Workspace relative or absolute path to the program.
			"program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
			// Automatically stop program after launch.
			"stopOnEntry": false,
			// Command line arguments passed to the program.
			"args": ["../client/server/test/suggest.test.js"],
			// Workspace relative or absolute path to the working directory of the program being debugged. Default is the current workspace.
			"cwd": "${workspaceRoot}",
			// Workspace relative or absolute path to the runtime executable to be used. Default is the runtime executable on the PATH.
			"runtimeExecutable": null,
			"outFiles": [ "${workspaceRoot}/../client/server/**/*.js" ],
			"sourceMaps": true,
			// Environment variables passed to the program.
			"env": { "NODE_ENV": "test"}
		},
        {
			// Name of configuration; appears in the launch configuration drop down menu.
			"name": "Run mocha WordDictionary.test",
			// Type of configuration. Possible values: "node", "mono".
			"type": "node",
            // Request type "launch" or "attach"
            "request": "launch",
			// Workspace relative or absolute path to the program.
			"program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
			// Automatically stop program after launch.
			"stopOnEntry": false,
			// Command line arguments passed to the program.
			"args": ["../client/server/test/WordDictionary.test.js"],
			// Workspace relative or absolute path to the working directory of the program being debugged. Default is the current workspace.
			"cwd": "${workspaceRoot}",
			// Workspace relative or absolute path to the runtime executable to be used. Default is the runtime executable on the PATH.
			"runtimeExecutable": null,
			"outFiles": [ "${workspaceRoot}/../client/server/**/*.js" ],
			"sourceMaps": true,
			// Environment variables passed to the program.
			"env": { "NODE_ENV": "test"}
		},
        {
			// Name of configuration; appears in the launch configuration drop down menu.
			"name": "Run mocha text.test",
			// Type of configuration. Possible values: "node", "mono".
			"type": "node2",
            // Request type "launch" or "attach"
            "request": "launch",
			// Workspace relative or absolute path to the program.
			"program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
			// Automatically stop program after launch.
			"stopOnEntry": false,
			// Command line arguments passed to the program.
			"args": ["../client/server/test/util/text.test.js"],
			// Workspace relative or absolute path to the working directory of the program being debugged. Default is the current workspace.
			"cwd": "${workspaceRoot}",
			// Workspace relative or absolute path to the runtime executable to be used. Default is the runtime executable on the PATH.
			"runtimeExecutable": null,
			"outFiles": [ "${workspaceRoot}/../client/server/**/*.js" ],
			"sourceMaps": true,
			// Environment variables passed to the program.
			"env": { "NODE_ENV": "test"}
		},
        {
			// Name of configuration; appears in the launch configuration drop down menu.
			"name": "Run mocha validator.test",
			// Type of configuration. Possible values: "node", "mono".
			"type": "node2",
            // Request type "launch" or "attach"
            "request": "launch",
			// Workspace relative or absolute path to the program.
			"program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
			// Automatically stop program after launch.
			"stopOnEntry": false,
			// Command line arguments passed to the program.
			"args": ["../client/server/test/validator.test.js"],
			// Workspace relative or absolute path to the working directory of the program being debugged. Default is the current workspace.
			"cwd": "${workspaceRoot}",
			// Workspace relative or absolute path to the runtime executable to be used. Default is the runtime executable on the PATH.
			"runtimeExecutable": null,
			"outFiles": [ "${workspaceRoot}/../client/server/**/*.js" ],
			"sourceMaps": true,
			// Environment variables passed to the program.
			"env": { "NODE_ENV": "test"}
		},
        {
			// Name of configuration; appears in the launch configuration drop down menu.
			"name": "Run mocha GenSequence Tests",
			// Type of configuration. Possible values: "node", "mono".
			"type": "node2",
            // Request type "launch" or "attach"
            "request": "launch",
			// Workspace relative or absolute path to the program.
			"program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
			// Automatically stop program after launch.
			"stopOnEntry": false,
			// Command line arguments passed to the program.
			"args": ["../client/server/test/util/GenSequence.test.js"],
			// Workspace relative or absolute path to the working directory of the program being debugged. Default is the current workspace.
			"cwd": "${workspaceRoot}",
			// Workspace relative or absolute path to the runtime executable to be used. Default is the runtime executable on the PATH.
			"runtimeExecutable": null,
			"outFiles": [ "${workspaceRoot}/../client/server/**/*.js" ],
			"sourceMaps": true,
			// Environment variables passed to the program.
			"env": { "NODE_ENV": "test"}
		},
        {
			// Name of configuration; appears in the launch configuration drop down menu.
			"name": "Run mocha wordListCompiler Tests",
			// Type of configuration. Possible values: "node", "mono".
			"type": "node2",
            // Request type "launch" or "attach"
            "request": "launch",
			// Workspace relative or absolute path to the program.
			"program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
			// Automatically stop program after launch.
			"stopOnEntry": false,
			// Command line arguments passed to the program.
			"args": ["../client/server/test/wordListCompiler.test.js"],
			// Workspace relative or absolute path to the working directory of the program being debugged. Default is the current workspace.
			"cwd": "${workspaceRoot}",
			// Workspace relative or absolute path to the runtime executable to be used. Default is the runtime executable on the PATH.
			"runtimeExecutable": null,
			"outFiles": [ "${workspaceRoot}/../client/server/**/*.js" ],
			"sourceMaps": true,
			// Environment variables passed to the program.
			"env": { "NODE_ENV": "test"}
		},
        {
			// Name of configuration; appears in the launch configuration drop down menu.
			"name": "Run Perf Tests",
			// Type of configuration. Possible values: "node", "mono".
			"type": "node2",
            // Request type "launch" or "attach"
            "request": "launch",
			// Workspace relative or absolute path to the program.
			"program": "${workspaceRoot}/node_modules/mocha/bin/_mocha",
			// Automatically stop program after launch.
			"stopOnEntry": false,
			// Command line arguments passed to the program.
			"args": ["--recursive", "../client/server/perf"],
			// Workspace relative or absolute path to the working directory of the program being debugged. Default is the current workspace.
			"cwd": "${workspaceRoot}",
			// Workspace relative or absolute path to the runtime executable to be used. Default is the runtime executable on the PATH.
			"runtimeExecutable": null,
			"outFiles": [ "${workspaceRoot}/../client/server/**/*.js" ],
			"sourceMaps": true,
			// Environment variables passed to the program.
			"env": { "NODE_ENV": "test"}
		}
	]
}