name: cspell-action
on:
  pull_request:
  push:
    branches:
      - master

jobs:
  cspell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: streetsidesoftware/cspell-action@v1.1.1
