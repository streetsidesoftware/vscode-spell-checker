{
    "version": "0.2.0",
    // List of configurations. Add new configurations or edit existing ones.
    "configurations": [
        {
            "name": "Server: Attach Server",
            "type": "node",
            "request": "attach",
            // TCP/IP address. Default is "localhost".
            "address": "localhost",
            "port": 60048,
            "sourceMaps": true,
            "outFiles": ["${workspaceFolder}/dist/**/*.js"],
            "protocol": "inspector",
            "smartStep": false,
            "skipFiles": ["<node_internals>/**"]
        }
    ]
}
