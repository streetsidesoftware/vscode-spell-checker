{
    "version": "0.2.0",
    // List of configurations. Add new configurations or edit existing ones.
    "configurations": [
        {
            "name": "Server Pattern Matcher: Attach Server",
            "type": "node",
            "request": "attach",
            // TCP/IP address. Default is "localhost".
            "address": "localhost",
            "port": 60148,
            "sourceMaps": true,
            "outFiles": [
                "${workspaceRoot}/dist/**/*.js"
            ],
            "protocol": "inspector"
        }
    ]
}
