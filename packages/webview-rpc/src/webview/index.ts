import { debug, error, getLogLevel, info, log, setLogLevel, warn } from '../common/logger.js';

export {} from '../common/json-rpc-api.js';

export const logger = {
    debug,
    error,
    getLogLevel,
    info,
    log,
    setLogLevel,
    warn,
};
